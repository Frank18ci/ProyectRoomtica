@model RoomticaFrontEnd.Models.CaracteristicaHabitacionModel

@{
    ViewData["Title"] = "Edit";
}

<div class="container-xl pb-4 pt-2">
    <h2>Editar Caracteristica Habitacion</h2>
    <hr />
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Caracteristica" class="control-label"></label>
                    <input asp-for="Caracteristica" class="form-control" />
                    <span asp-validation-for="Caracteristica" class="text-danger"></span>
                </div>
                <div class="form-group mt-2">
                    <input type="submit" value="Editar" class="btn btn-primary" />
                    <a asp-action="Listar" class="btn btn-warning">Volver al Listado</a>
                </div>
            </form>
        </div>
    </div>
    <div>
        <span class="text-danger fw-bold">@ViewBag.mensaje</span>
    </div>
    <div>
    </div>

    @section Scripts {
        @{
            await Html.RenderPartialAsync("_ValidationScriptsPartial");
        }
    }

</div>